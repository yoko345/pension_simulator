---
description: PR 依頼時は定型見出しの本文のみ返す。gh/git のコマンド例や PR 作成手順は出さない。docs/pull-request-guide.md を参照。
alwaysApply: true
---

# Pull Request 説明の形式

このリポジトリ（`pension_simulator`）では、PR 本文は次の **Markdown 見出し**で構成する。

1. **概要** — 何を達成するか（短く）
2. **背景** — 動機・Issue 等
3. **変更内容** — 具体差分、必要ならサブセクション
4. **動作確認** — チェックリスト（`npm run dev` / `npm run build` 等）

（個人開発のため「スコープ外」は設けない。意図が必要なときだけ本文に書く。）

**参照**: `docs/pull-request-guide.md`

GitHub 用の `.github/pull_request_template.md` は使わない。

## 出力のしかた（重要）

ユーザーが **PR を作成する**、**PR 本文を書いて**、**コピペ用**などと依頼したときは、**コピペ用の PR 本文（上記見出しの Markdown）だけ**を返す。先頭からそのまま GitHub の説明欄に貼れる形にする。

**出さない（ユーザーが明示的に依頼した場合を除く）**

- `gh pr create` を含む **GitHub CLI のコマンド全文**
- `git push` / `git commit` など **ターミナルで PR を作るための手順一式**
- PR のタイトル案（必要ならユーザーが本文から一文で足す想定）
- 長い操作説明

PR の作成・プッシュ・マージは **ユーザーが GitHub の UI や手元の git で行う**。エージェントは **本文テキストのドラフトまで**とする。

プレースホルダや `<!-- -->` は使わず、実内容で書く。
